"use client"

/**
 * Case study detail page.
 * Returns null until real client data exists (measurable outcome, quote, before/after).
 * Structure for future: Challenge, Approach (bullets), Result (metric), quote, link to live site.
 */
const hasRealCaseStudyData = false

export default function WorkCaseStudyPage() {
    if (!hasRealCaseStudyData) return null

    return null
}
